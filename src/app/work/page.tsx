import Link from "next/link";import {ArrowUpRight} from "lucide-react";import {SiteFooter,SiteHeader} from "@/components/SiteHeader";import {workItems} from "@/lib/content";
export const metadata={title:"Work archive — Fraser Brown",description:"An anonymised account of the mechanisms and ideas developed through Fraser Brown's conservation-finance work."};
export default function Work(){return <><SiteHeader/><main><section className="page-intro"><p className="eyebrow">The archive / 2014–2018</p><h1>A record of trying to make restoration <em>financeable.</em></h1><p>Ideas from private client work, lightly framed and honestly labelled. The source papers are not published; this archive keeps only the public-facing concepts they informed.</p></section><section className="archive-intro"><p className="eyebrow">How to read this archive</p><p>Client names, confidential papers, filenames and internal details are deliberately omitted. These entries are thematic summaries, not document reproductions or claims that any client endorsed the current proposal.</p><p><Link className="text-link" href="/work/prior-art">See the public prior-art directory →</Link></p></section><section className="work-list">{workItems.map(item=><article className="work-row" key={item.slug}><span className="year">{item.year}</span><div><h3>{item.title}</h3><p>{item.summary}</p></div><div><p>{item.client}</p><div className="tags">{item.tags.map(tag=><span key={tag}>{tag}</span>)}</div></div><Link className="row-open" href={`/work/${item.slug}`}>Open ↗</Link></article>)}</section></main><SiteFooter/></>}