import Link from "next/link";import {ArrowUpRight} from "lucide-react";import {SiteFooter,SiteHeader} from "@/components/SiteHeader";import {workItems} from "@/lib/content";
export default function Work(){return <><SiteHeader/><main><section className="page-intro"><p className="eyebrow">The archive / 2014–2018</p><h1>A record of trying to make restoration <em>financeable.</em></h1><p>Working documents, lightly framed and honestly labelled. The argument on this site rests on these attempts.</p></section><section className="archive-intro"><p className="eyebrow">How to read this archive</p><p>Most documents were written for a client, partner or regulator. Several were explicitly not validated or approved by the institutions named in them. They are here because they show the direction of travel: funds, platforms, verification, and incentives.</p></section><section className="work-list">{workItems.map(item=><article className="work-row" key={item.slug}><span className="year">{item.year}</span><div><h3>{item.title}</h3><p>{item.summary}</p></div><div><p>{item.client}</p><div className="tags">{item.tags.map(tag=><span key={tag}>{tag}</span>)}</div></div><Link className="row-open" href={`/work/${item.slug}`}>Open ↗</Link></article>)}</section></main><SiteFooter/></>}