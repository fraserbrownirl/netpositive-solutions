import Link from "next/link";import {notFound} from "next/navigation";import {SiteFooter,SiteHeader} from "@/components/SiteHeader";import {workItems} from "@/lib/content";
export function generateStaticParams(){return workItems.map(item=>({slug:item.slug}))}
export default function WorkDetail({params}:{params:{slug:string}}){const item=workItems.find(entry=>entry.slug===params.slug);if(!item)return notFound();return <><SiteHeader/><main><article className="article-page"><p className="eyebrow">Archive / {item.year} / {item.client}</p><h1>{item.title}</h1><p className="dek">{item.summary}</p><div className="tags">{item.tags.map(tag=><span key={tag}>{tag}</span>)}</div><div className="body"><p>This document is part of the Net-Positive Solutions archive. It records a practical attempt to move money, evidence and decision-making closer to the people doing restoration.</p><p>The public page currently provides context and metadata while publication consent, redaction and source-file handling are confirmed. The argument is stronger when the record is available, but it must be available honestly.</p><div className="callout"><strong>Publication status</strong><br/>Working document. Confirm permissions and attach the approved PDF before launch.</div><p><Link className="button" href="/work">← Back to the archive</Link></p></div></article></main><SiteFooter/></>}