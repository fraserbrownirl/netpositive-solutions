import Link from "next/link";import {SiteFooter,SiteHeader} from "@/components/SiteHeader";import {writingItems} from "@/lib/content";
export default function Writing(){return <><SiteHeader/><main><section className="page-intro"><p className="eyebrow">Writing / notes</p><h1>Ideas from the<br/><em>working edge.</em></h1><p>Short essays on restoration finance, verification and the political economy of getting useful work paid for.</p></section><section className="writing-wrap"><div className="writing-grid">{writingItems.map(item=><Link className="card" href={`/writing/${item.slug}`} key={item.slug}><span className="meta">{item.date} · note</span><h3>{item.title}</h3><p>{item.excerpt}</p><span className="text-link">Read note →</span></Link>)}</div></section></main><SiteFooter/></>}