import Link from "next/link";import {notFound} from "next/navigation";import {SiteFooter,SiteHeader} from "@/components/SiteHeader";import {writingItems} from "@/lib/content";
export const metadata={title:"Writing note — Fraser Brown",description:"A working note from Net-Positive Solutions."};
export function generateStaticParams(){return writingItems.map(item=>({slug:item.slug}))}
export default async function WritingDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=writingItems.find(entry=>entry.slug===slug);if(!item)return notFound();return <><SiteHeader/><main><article className="article-page"><p className="eyebrow">Writing / {item.date}</p><h1>{item.title}</h1><p className="dek">{item.excerpt}</p><div className="body"><p>This is a launch placeholder for the working essay. The final version will be written in Fraser’s voice and cross-link to the relevant archive document.</p><p>The point of publishing the note is not to present a finished product. It is to make the design question concrete enough that practitioners, funders and technical builders can challenge it.</p><div className="callout">Want to respond? <Link href="/community">Join the conservation finance community →</Link></div><p><Link className="button" href="/writing">← Back to writing</Link></p></div></article></main><SiteFooter/></>}